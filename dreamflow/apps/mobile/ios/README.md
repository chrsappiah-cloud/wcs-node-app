# iOS Workspace

This folder is reserved for the native iOS workspace generated by React Native CLI.

Recommended path:

1. Run React Native initialization in `apps/mobile`.
2. Open the generated `.xcworkspace` or `.xcodeproj` in Xcode.
3. Continue native permissions/background location/push setup in Xcode.
